services:
  api:
    build: .
    container_name: api
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL:-sqlite:////data/app.db}
      - WEBHOOK_SECRET=${WEBHOOK_SECRET}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      - data:/data

volumes:
  data:
